// // src/utils/loadImages.js
// const loadImages = (context) => {
//   let images = {};
//   context.keys().forEach((item) => {
//     images[item.replace("./", "")] = context(item).default;
//   });
//   return images;
// };

// export default loadImages;
